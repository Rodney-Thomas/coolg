function plusApi()
{
  title: (setTitle)=>{
    var titleDone = setTitle;
  },
   description: (setDesc)=>{
     var descDone = setDesc;
   },
   version: (setVer)=>{
     var verDone = setVer;
   },
   start: (setDiv)=>{
     var divDone = setDiv;
   }
}
